<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- private int resCancel,resCode;
	private String resCosname,resMydate,resId,resName,resEmail,resPhone,resAddress,resPlaytime,resResdate; -->
<mapper namespace="site.nicezo.mapper.ResCosTimeMapper">
	<select id="costimeList" resultType="ResCosTime">
		select * from res_costime 
	</select>
</mapper>